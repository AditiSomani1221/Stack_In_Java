public class A_Basic {
/*
 * Stacks are created explictly that means we can create stack unlike it is created in recursion
 * there are 3 oprration in stack
 * push -instering one element at top
 * pop-removing first element from top
 * peek-to seek in
 * all of them can be done in O(1)
 * stack follow Last in first out
 * we can create a stack in
 * array- fixed size stack
 * arrayList-variable size
 * LinkedList- variable size
 */
}
